[alias]
xtask = "run --quiet --package xtask --"

[build]
# 使用所有 CPU 核心 (24核) 进行并行编译
jobs = 24

# 启用增量编译 - 大幅提升重编译速度
incremental = true

# 使用更快的链接器
[target.x86_64-unknown-linux-gnu]
linker = "clang"
rustflags = ["-C", "link-arg=-fuse-ld=mold"]
