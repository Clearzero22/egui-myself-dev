# ClipHub Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Build a clipboard history manager that automatically captures content and syncs to Notion/self-hosted server/Obsidian.

**Architecture:** Local-first SQLite storage with background clipboard monitoring. Modular sync system with separate clients for each platform. egui-based desktop UI with system tray integration.

**Tech Stack:** Rust, egui/eframe, arboard (clipboard), rusqlite (database), tokio (async), reqwest (HTTP), serde (serialization)

---

## Project Setup

### Task 1: Create Project Structure

**Files:**
- Create: `cliphub/Cargo.toml`
- Create: `cliphub/src/main.rs`

**Step 1: Create Cargo.toml**

```toml
[package]
name = "cliphub"
version = "0.1.0"
edition = "2021"

[dependencies]
eframe = "0.33"
egui = "0.33"
arboard = "3.6"
rusqlite = { version = "0.32", features = ["bundled"] }
tokio = { version = "1.47", features = ["full"] }
reqwest = { version = "0.12", features = ["json"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = { version = "0.4", features = ["serde"] }
anyhow = "1.0"
thiserror = "1.0"
```

**Step 2: Create main.rs with basic window**

```rust
use eframe::egui;

fn main() -> eframe::Result<()> {
    let options = eframe::NativeOptions {
        viewport: egui::ViewportBuilder::default()
            .with_inner_size([800.0, 600.0])
            .with_title("ClipHub"),
        ..Default::default()
    };
    
    eframe::run_native(
        "ClipHub",
        options,
        Box::new(|_cc| Box::<ClipHubApp>::default()),
    )
}

struct ClipHubApp;

impl Default for ClipHubApp {
    fn default() -> Self {
        Self
    }
}

impl eframe::App for ClipHubApp {
    fn update(&mut self, ctx: &egui::Context, _frame: &mut eframe::Frame) {
        egui::CentralPanel::default().show(ctx, |ui| {
            ui.heading("📋 ClipHub");
            ui.label("Clipboard history manager - coming soon!");
        });
    }
}
```

**Step 3: Test run**

Run: `cargo run --bin cliphub`
Expected: Window opens with "ClipHub" heading

**Step 4: Commit**

```bash
git add cliphub/
git commit -m "feat(cliphub): initialize project with basic window"
```

---

## Core Data Structures

### Task 2: Define Data Models

**Files:**
- Create: `cliphub/src/types.rs`
- Modify: `cliphub/src/main.rs`

**Step 1: Create types.rs**

```rust
use chrono::{DateTime, Utc};
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub enum ContentType {
    Text,
    Code { language: String },
    Image { path: String, width: u32, height: u32 },
    Url { url: String, title: Option<String> },
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ClipboardItem {
    pub id: Option<i64>,
    pub content_type: ContentType,
    pub title: Option<String>,
    pub content: String,
    pub source_app: Option<String>,
    pub created_at: DateTime<Utc>,
    pub is_synced: bool,
    pub sync_targets: Vec<String>,
    pub tags: Vec<String>,
}

impl Default for ClipboardItem {
    fn default() -> Self {
        Self {
            id: None,
            content_type: ContentType::Text,
            title: None,
            content: String::new(),
            source_app: None,
            created_at: Utc::now(),
            is_synced: false,
            sync_targets: Vec::new(),
            tags: Vec::new(),
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum SyncTarget {
    Notion,
    Server,
    Obsidian,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct SyncStatus {
    pub item_id: i64,
    pub target: SyncTarget,
    pub status: SyncState,
    pub error_message: Option<String>,
    pub last_sync_at: Option<DateTime<Utc>>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub enum SyncState {
    Pending,
    Synced,
    Failed,
}
```

**Step 2: Add mod to main.rs**

```rust
mod types;
use types::ClipboardItem;
```

**Step 3: Test compile**

Run: `cargo check -p cliphub`
Expected: No errors

**Step 4: Commit**

```bash
git add cliphub/src/
git commit -m "feat(cliphub): define core data types"
```

---

## Storage Layer

### Task 3: Database Schema

**Files:**
- Create: `cliphub/src/storage/database.rs`
- Create: `cliphub/src/storage/mod.rs`

**Step 1: Create storage/mod.rs**

```rust
pub mod database;

pub use database::Database;
```

**Step 2: Create storage/database.rs (schema setup)**

```rust
use rusqlite::{Connection, Result as SqliteResult};
use anyhow::Result;
use std::path::Path;

pub struct Database {
    conn: Connection,
}

impl Database {
    pub fn new<P: AsRef<Path>>(path: P) -> Result<Self> {
        let conn = Connection::open(path)?;
        let db = Self { conn };
        db.init_schema()?;
        Ok(db)
    }

    pub fn in_memory() -> Result<Self> {
        let conn = Connection::open_in_memory()?;
        let db = Self { conn };
        db.init_schema()?;
        Ok(db)
    }

    fn init_schema(&self) -> Result<()> {
        self.conn.execute(
            "CREATE TABLE IF NOT EXISTS clipboard_items (
                id              INTEGER PRIMARY KEY AUTOINCREMENT,
                content_type    TEXT NOT NULL,
                title           TEXT,
                content         TEXT NOT NULL,
                metadata        TEXT,
                source_app      TEXT,
                created_at      TEXT NOT NULL,
                is_synced       BOOLEAN DEFAULT 0,
                sync_targets    TEXT,
                tags            TEXT,
                is_deleted      BOOLEAN DEFAULT 0
            )",
            [],
        )?;

        self.conn.execute(
            "CREATE TABLE IF NOT EXISTS sync_status (
                id              INTEGER PRIMARY KEY AUTOINCREMENT,
                item_id         INTEGER REFERENCES clipboard_items(id),
                target          TEXT NOT NULL,
                status          TEXT NOT NULL,
                error_message   TEXT,
                last_sync_at    TEXT
            )",
            [],
        )?;

        Ok(())
    }
}
```

**Step 3: Add storage module to main.rs**

```rust
mod storage;
use storage::Database;
```

**Step 4: Test compile**

Run: `cargo check -p cliphub`
Expected: No errors

**Step 5: Write database test**

Create: `cliphub/tests/storage_test.rs`

```rust
use cliphub::storage::Database;

#[tokio::test]
async fn test_database_creation() {
    let db = Database::in_memory().unwrap();
    
    // Verify tables exist
    let table_count: i64 = db.conn.query_row(
        "SELECT COUNT(*) FROM sqlite_master WHERE type='table' AND name IN ('clipboard_items', 'sync_status')",
        [],
        |row| row.get(0),
    ).unwrap();
    
    assert_eq!(table_count, 2);
}
```

**Step 6: Run test**

Run: `cargo test -p cliphub test_database_creation`
Expected: PASS

**Step 7: Commit**

```bash
git add cliphub/
git commit -m "feat(cliphub): add database schema and initialization"
```

---

### Task 4: Database CRUD Operations

**Files:**
- Modify: `cliphub/src/storage/database.rs`

**Step 1: Add insert method**

```rust
use crate::types::{ClipboardItem, ContentType, SyncState};
use chrono::Utc;

impl Database {
    pub fn insert_item(&self, item: &ClipboardItem) -> Result<i64> {
        let content_type = match &item.content_type {
            ContentType::Text => "text",
            ContentType::Code { language } => format!("code:{}", language),
            ContentType::Image { path, .. } => format!("image:{}", path),
            ContentType::Url { url, .. } => format!("url:{}", url),
        };

        let sync_targets = serde_json::to_string(&item.sync_targets)?;
        let tags = serde_json::to_string(&item.tags)?;

        self.conn.execute(
            "INSERT INTO clipboard_items (content_type, title, content, source_app, created_at, sync_targets, tags)
             VALUES (?1, ?2, ?3, ?4, ?5, ?6, ?7)",
            (
                &content_type,
                item.title.as_deref(),
                &item.content,
                item.source_app.as_deref(),
                &item.created_at.to_rfc3339(),
                &sync_targets,
                &tags,
            ),
        )?;

        Ok(self.conn.last_insert_rowid())
    }

    pub fn get_recent_items(&self, limit: usize) -> Result<Vec<ClipboardItem>> {
        let mut stmt = self.conn.prepare(
            "SELECT id, content_type, title, content, source_app, created_at, sync_targets, tags
             FROM clipboard_items
             WHERE is_deleted = 0
             ORDER BY created_at DESC
             LIMIT ?1"
        )?;

        let items = stmt.query_map([limit as i64], |row| {
            let content_type_str: String = row.get(1)?;
            let content_type = Self::parse_content_type(&content_type_str);
            
            let created_at_str: String = row.get(5)?;
            let created_at = DateTime::parse_from_rfc3339(&created_at_str)
                .unwrap()
                .with_timezone(&Utc);

            let sync_targets_str: String = row.get(6)?;
            let sync_targets = serde_json::from_str(&sync_targets_str).unwrap_or_default();

            let tags_str: String = row.get(7)?;
            let tags = serde_json::from_str(&tags_str).unwrap_or_default();

            Ok(ClipboardItem {
                id: Some(row.get(0)?),
                content_type,
                title: row.get(2)?,
                content: row.get(3)?,
                source_app: row.get(4)?,
                created_at,
                is_synced: false, // TODO: query from sync_status
                sync_targets,
                tags,
            })
        })?;

        items.collect()
    }

    fn parse_content_type(s: &str) -> ContentType {
        if let Some(rest) = s.strip_prefix("code:") {
            ContentType::Code { language: rest.to_string() }
        } else if let Some(rest) = s.strip_prefix("image:") {
            ContentType::Image { path: rest.to_string(), width: 0, height: 0 }
        } else if let Some(rest) = s.strip_prefix("url:") {
            ContentType::Url { url: rest.to_string(), title: None }
        } else {
            ContentType::Text
        }
    }
}
```

**Step 2: Write test for insert and query**

Create: `cliphub/tests/crud_test.rs`

```rust
use cliphub::storage::Database;
use cliphub::types::{ClipboardItem, ContentType};

#[tokio::test]
async fn test_insert_and_query() {
    let db = Database::in_memory().unwrap();
    
    let item = ClipboardItem {
        content_type: ContentType::Text,
        title: Some("Test Item".to_string()),
        content: "Test content".to_string(),
        source_app: Some("TestApp".to_string()),
        ..Default::default()
    };

    let id = db.insert_item(&item).unwrap();
    assert!(id > 0);

    let items = db.get_recent_items(10).unwrap();
    assert_eq!(items.len(), 1);
    assert_eq!(items[0].title, Some("Test Item".to_string()));
    assert_eq!(items[0].content, "Test content");
}
```

**Step 3: Run test**

Run: `cargo test -p cliphub test_insert_and_query`
Expected: FAIL (compile errors first, fix them)

**Step 4: Fix any compilation errors and rerun**

Expected: PASS

**Step 5: Commit**

```bash
git add cliphub/
git commit -m "feat(cliphub): add database CRUD operations"
```

---

## Clipboard Monitoring

### Task 5: Clipboard Detector

**Files:**
- Create: `cliphub/src/clipboard/mod.rs`
- Create: `cliphub/src/clipboard/detector.rs`

**Step 1: Create clipboard/mod.rs**

```rust
pub mod detector;

pub use detector::{detect_content_type, ContentTypeDetection};
```

**Step 2: Create detector.rs**

```rust
use crate::types::ContentType;
use std::collections::HashSet;

pub struct ContentTypeDetection {
    pub content_type: ContentType,
    pub language: Option<String>,
    pub is_url: bool,
}

pub fn detect_content_type(content: &str) -> ContentTypeDetection {
    // Check for URL first
    if is_url(content) {
        return ContentTypeDetection {
            content_type: ContentType::Url { 
                url: content.to_string(), 
                title: None 
            },
            language: None,
            is_url: true,
        };
    }

    // Check for code patterns
    if let Some(lang) = detect_code_language(content) {
        return ContentTypeDetection {
            content_type: ContentType::Code { language: lang.clone() },
            language: Some(lang),
            is_url: false,
        };
    }

    // Default to text
    ContentTypeDetection {
        content_type: ContentType::Text,
        language: None,
        is_url: false,
    }
}

fn is_url(text: &str) -> bool {
    text.starts_with("http://") || text.starts_with("https://")
}

fn detect_code_language(content: &str) -> Option<String> {
    let code_patterns = [
        (r#"fn\s+\w+\s*\("#, "rust"),
        (r#"def\s+\w+"\s*\("#, "python"),
        (r#"public\s+(static\s+)?class"#, "java"),
        (r#"function\s+\w+\s*\("#, "javascript"),
        (r#"interface\s+\w+"#, "typescript"),
        (r#"import\s+"#, "python"),
        (r#"use\s+"#, "rust"),
        (r#"const\s+\w+\s*="#, "javascript"),
    ];

    let lower_content = content.to_lowercase();
    
    // Quick heuristics
    if lower_content.contains("fn main") || lower_content.contains("pub fn") {
        return Some("rust".to_string());
    }
    if lower_content.contains("def ") && lower_content.contains(":") {
        return Some("python".to_string());
    }
    if lower_content.contains("function ") || lower_content.contains("=>") {
        return Some("javascript".to_string());
    }

    None
}
```

**Step 3: Write detector tests**

Create: `cliphub/tests/detector_test.rs`

```rust
use cliphub::clipboard::detect_content_type;

#[test]
fn test_detect_rust_code() {
    let code = r#"fn main() {
        println!("Hello");
    }"#;
    
    let result = detect_content_type(code);
    assert!(result.language.as_ref().is_some_and(|l| l == "rust"));
}

#[test]
fn test_detect_url() {
    let url = "https://example.com";
    let result = detect_content_type(url);
    assert!(result.is_url);
}

#[test]
fn test_detect_plain_text() {
    let text = "Just some plain text";
    let result = detect_content_type(text);
    assert!(!result.is_url);
    assert!(result.language.is_none());
}
```

**Step 4: Run tests**

Run: `cargo test -p cliphub detector`
Expected: All PASS

**Step 5: Commit**

```bash
git add cliphub/
git commit -m "feat(cliphub): add content type detection"
```

---

### Task 6: Clipboard Monitor

**Files:**
- Create: `cliphub/src/clipboard/monitor.rs`

**Step 1: Create monitor.rs**

```rust
use arboard::Clipboard;
use std::time::Duration;
use tokio::time::interval;
use crate::types::ClipboardItem;
use crate::clipboard::detector::detect_content_type;
use crate::storage::Database;
use anyhow::Result;
use chrono::Utc;

pub struct ClipboardMonitor {
    last_content: String,
    interval: Duration,
}

impl ClipboardMonitor {
    pub fn new(interval: Duration) -> Self {
        Self {
            last_content: String::new(),
            interval,
        }
    }

    pub async fn run(&mut self, db: &Database) -> Result<()> {
        let mut timer = interval(self.interval);
        let mut clipboard = Clipboard::new()?;

        loop {
            timer.tick().await;

            if let Ok(content) = clipboard.get_text() {
                if content != self.last_content && !content.is_empty() {
                    let detection = detect_content_type(&content);
                    
                    let item = ClipboardItem {
                        id: None,
                        content_type: detection.content_type,
                        title: generate_title(&content, &detection),
                        content,
                        source_app: detect_source_app(),
                        created_at: Utc::now(),
                        is_synced: false,
                        sync_targets: Vec::new(),
                        tags: Vec::new(),
                    };

                    if let Ok(id) = db.insert_item(&item) {
                        println!("Captured clipboard item: {}", id);
                    }

                    self.last_content = content.clone();
                }
            }
        }
    }
}

fn generate_title(content: &str, detection: &crate::clipboard::ContentTypeDetection) -> Option<String> {
    let preview = content.lines().next().unwrap_or("");
    let truncated = if preview.len() > 50 {
        format!("{}...", &preview[..50])
    } else {
        preview.to_string()
    };

    if let Some(lang) = &detection.language {
        Some(format!("{} - {}", lang.to_uppercase(), truncated))
    } else {
        Some(truncated)
    }
}

fn detect_source_app() -> Option<String> {
    // Platform-specific implementation
    #[cfg(target_os = "linux")]
    {
        // Use x11/wayland to detect active window
        None // TODO: implement
    }
    
    #[cfg(target_os = "macos")]
    {
        None // TODO: use NSWorkspace
    }
    
    #[cfg(target_os = "windows")]
    {
        None // TODO: use GetForegroundWindow
    }
    
    None
}
```

**Step 2: Add monitor module**

Modify: `cliphub/src/clipboard/mod.rs`

```rust
pub mod detector;
pub mod monitor;

pub use detector::{detect_content_type, ContentTypeDetection};
pub use monitor::ClipboardMonitor;
```

**Step 3: Add tokio to main.rs for testing**

Modify: `cliphub/src/main.rs`

```rust
mod types;
mod storage;
mod clipboard;

use clipboard::ClipboardMonitor;
use storage::Database;

fn main() -> eframe::Result<()> {
    // Initialize database
    let db = Database::new("cliphub.db").expect("Failed to open database");
    
    // Start clipboard monitor in background
    tokio::spawn(async move {
        let mut monitor = ClipboardMonitor::new(Duration::from_secs(1));
        if let Err(e) = monitor.run(&db).await {
            eprintln!("Clipboard monitor error: {}", e);
        }
    });

    // ... rest of main
```

**Step 4: Test compile**

Run: `cargo check -p cliphub`
Expected: May have lifetime errors, fix them

**Step 5: Commit**

```bash
git add cliphub/
git commit -m "feat(cliphub): add clipboard monitoring"
```

---

## UI Components

### Task 7: Main Window Layout

**Files:**
- Modify: `cliphub/src/main.rs`
- Create: `cliphub/src/ui/main_window.rs`

**Step 1: Create ui module**

Create: `cliphub/src/ui/mod.rs`

```rust
pub mod main_window;

pub use main_window::MainWindow;
```

**Step 2: Create main_window.rs**

```rust
use crate::types::ClipboardItem;
use crate::storage::Database;

pub struct MainWindow {
    items: Vec<ClipboardItem>,
    search_query: String,
    selected_index: Option<usize>,
    db: Database,
}

impl MainWindow {
    pub fn new(db: Database) -> Self {
        let items = db.get_recent_items(50).unwrap_or_default();
        
        Self {
            items,
            search_query: String::new(),
            selected_index: None,
            db,
        }
    }

    pub fn refresh_items(&mut self) {
        self.items = self.db.get_recent_items(50).unwrap_or_default();
    }

    pub fn show(&mut self, ctx: &egui::Context) {
        egui::TopBottomPanel::top("header").show(ctx, |ui| {
            ui.horizontal(|ui| {
                ui.heading("📋 ClipHub");
                ui.separator();
                if ui.button("🔄 Refresh").clicked() {
                    self.refresh_items();
                }
            });
        });

        egui::TopBottomPanel::top("search").show(ctx, |ui| {
            ui.horizontal(|ui| {
                ui.label("🔍");
                ui.text_edit_singleline(&mut self.search_query);
            });
        });

        egui::CentralPanel::default().show(ctx, |ui| {
            self.show_item_list(ui);
        });
    }

    fn show_item_list(&mut self, ui: &mut egui::Ui) {
        let filtered: Vec<_> = self.items
            .iter()
            .filter(|item| {
                self.search_query.is_empty() || 
                item.content.to_lowercase().contains(&self.search_query.to_lowercase()) ||
                item.title.as_ref().map(|t| t.to_lowercase().contains(&self.search_query.to_lowercase())).unwrap_or(false)
            })
            .enumerate()
            .collect();

        for (idx, item) in filtered {
            let is_selected = self.selected_index == Some(idx);
            
            egui::Frame::none()
                .fill(if is_selected { ui.visuals().faint_bg_color } else { egui::Color32::TRANSPARENT })
                .show(ui, |ui| {
                    self.show_item_card(ui, item);
                });
        }
    }

    fn show_item_card(&self, ui: &mut egui::Ui, item: &ClipboardItem) {
        ui.vertical(|ui| {
            // Title row
            ui.horizontal(|ui| {
                let icon = match &item.content_type {
                    crate::types::ContentType::Text => "📄",
                    crate::types::ContentType::Code { .. } => "💻",
                    crate::types::ContentType::Image { .. } => "🖼️",
                    crate::types::ContentType::Url { .. } => "🔗",
                };
                ui.label(format!("{} {}", icon, item.title.as_ref().unwrap_or(&"Untitled".to_string())));
                
                ui.with_layout(egui::Layout::right_to_left(egui::Align::Center), |ui| {
                    ui.label(format!("{}", item.created_at.format("%H:%M")));
                });
            });

            // Content preview
            let preview = if item.content.len() > 100 {
                format!("{}...", &item.content[..100])
            } else {
                item.content.clone()
            };
            ui.label(egui::RichText::new(preview).small().weak());

            // Tags
            if !item.tags.is_empty() {
                ui.horizontal(|ui| {
                    for tag in &item.tags {
                        ui.label(egui::RichText::new(format!("🏷️ {}", tag)).small());
                    }
                });
            }
        });
        ui.separator();
    }
}
```

**Step 3: Update main.rs to use MainWindow**

```rust
mod types;
mod storage;
mod clipboard;
mod ui;

use ui::MainWindow;
use storage::Database;

struct ClipHubApp {
    main_window: MainWindow,
}

impl Default for ClipHubApp {
    fn default() -> Self {
        let db = Database::new("cliphub.db").expect("Failed to open database");
        Self {
            main_window: MainWindow::new(db),
        }
    }
}

impl eframe::App for ClipHubApp {
    fn update(&mut self, ctx: &egui::Context, _frame: &mut eframe::Frame) {
        self.main_window.show(ctx);
    }
}
```

**Step 4: Test run**

Run: `cargo run -p cliphub`
Expected: Window opens with empty list (no items yet)

**Step 5: Commit**

```bash
git add cliphub/
git commit -m "feat(cliphub): add main window UI"
```

---

## Integration

### Task 8: Connect Monitor to UI

**Files:**
- Modify: `cliphub/src/main.rs`

**Step 1: Add channel for clipboard events**

```rust
use std::sync::mpsc;
use std::thread;
use std::time::Duration;

struct ClipHubApp {
    main_window: MainWindow,
    receiver: mpsc::Receiver<ClipboardItem>,
}

impl Default for ClipHubApp {
    fn default() -> Self {
        let db = Database::new("cliphub.db").expect("Failed to open database");
        let main_window = MainWindow::new(db);
        
        let (sender, receiver) = mpsc::channel();
        
        // Start monitor thread
        thread::spawn(move || {
            let rt = tokio::runtime::Runtime::new().unwrap();
            let db = Database::new("cliphub.db").expect("Failed to open database");
            let mut monitor = ClipboardMonitor::new(Duration::from_secs(1));
            
            rt.block_on(async {
                // Override monitor to send through channel
                // For now, just run the monitor
                let _ = monitor.run(&db).await;
            });
        });

        Self { main_window, receiver }
    }
}

impl eframe::App for ClipHubApp {
    fn update(&mut self, ctx: &egui::Context, _frame: &mut eframe::Frame) {
        // Check for new clipboard items
        if let Ok(item) = self.receiver.try_recv() {
            self.main_window.refresh_items();
        }

        // Request repaint for continuous updates
        ctx.request_repaint();
        self.main_window.show(ctx);
    }
}
```

**Step 2: Test clipboard capture**

Run: `cargo run -p cliphub`
Copy some text
Expected: Items appear in list

**Step 3: Commit**

```bash
git add cliphub/
git commit -m "feat(cliphub): connect clipboard monitor to UI"
```

---

## Sync Module (Server First)

### Task 9: Server Sync Client

**Files:**
- Create: `cliphub/src/sync/mod.rs`
- Create: `cliphub/src/sync/server.rs`

**Step 1: Create sync/mod.rs**

```rust
pub mod server;

pub use server::ServerClient;
```

**Step 2: Create server.rs**

```rust
use crate::types::ClipboardItem;
use anyhow::Result;
use reqwest::Client;
use serde::Serialize;

#[derive(Debug)]
pub struct ServerClient {
    base_url: String,
    api_key: String,
    client: Client,
}

#[derive(Serialize)]
struct UploadRequest {
    title: Option<String>,
    content: String,
    content_type: String,
    tags: Vec<String>,
    created_at: String,
}

impl ServerClient {
    pub fn new(base_url: String, api_key: String) -> Self {
        Self {
            base_url,
            api_key,
            client: Client::new(),
        }
    }

    pub async fn upload(&self, item: &ClipboardItem) -> Result<()> {
        let url = format!("{}/api/clipboard", self.base_url);
        
        let request = UploadRequest {
            title: item.title.clone(),
            content: item.content.clone(),
            content_type: format!("{:?}", item.content_type),
            tags: item.tags.clone(),
            created_at: item.created_at.to_rfc3339(),
        };

        let response = self.client
            .post(&url)
            .header("Authorization", format!("Bearer {}", self.api_key))
            .json(&request)
            .send()
            .await?;

        if response.status().is_success() {
            Ok(())
        } else {
            anyhow::bail!("Upload failed: {}", response.status())
        }
    }
}
```

**Step 3: Write server test**

Create: `cliphub/tests/server_test.rs`

```rust
use cliphub::sync::ServerClient;
use cliphub::types::{ClipboardItem, ContentType};

#[tokio::test]
async fn test_server_client_upload() {
    // Use mock server or actual test server
    let client = ServerClient::new(
        "http://localhost:3000".to_string(),
        "test-key".to_string(),
    );

    let item = ClipboardItem {
        content_type: ContentType::Text,
        content: "Test content".to_string(),
        ..Default::default()
    };

    // This will fail without server - just verify it compiles
    let _ = client.upload(&item).await;
}
```

**Step 4: Test compile**

Run: `cargo check -p cliphub`
Expected: No errors

**Step 5: Commit**

```bash
git add cliphub/
git commit -m "feat(cliphub): add server sync client"
```

---

## Final Polish

### Task 10: Error Handling UI

**Files:**
- Modify: `cliphub/src/ui/main_window.rs`

**Step 1: Add error display**

```rust
pub struct MainWindow {
    // ... existing fields
    errors: Vec<String>,
}

impl MainWindow {
    pub fn show(&mut self, ctx: &egui::Context) {
        // Show errors if any
        if !self.errors.is_empty() {
            egui::Window::new("Errors")
                .collapsible(false)
                .resizable(false)
                .show(ctx, |ui| {
                    for error in &self.errors {
                        ui.colored_label(egui::Color32::RED, error);
                    }
                    if ui.button("Dismiss").clicked() {
                        self.errors.clear();
                    }
                });
        }

        // ... rest of show()
    }
}
```

**Step 2: Commit**

```bash
git add cliphub/
git commit -m "feat(cliphub): add error display UI"
```

---

## Testing & Documentation

### Task 11: Integration Test

**Files:**
- Create: `cliphub/tests/integration_test.rs`

**Step 1: Write E2E test**

```rust
use cliphub::storage::Database;
use cliphub::types::{ClipboardItem, ContentType};

#[tokio::test]
async fn test_full_workflow() {
    // 1. Create database
    let db = Database::in_memory().unwrap();

    // 2. Insert item
    let item = ClipboardItem {
        content_type: ContentType::Text,
        title: Some("Integration Test".to_string()),
        content: "Test content for integration test".to_string(),
        ..Default::default()
    };

    let id = db.insert_item(&item).unwrap();
    assert!(id > 0);

    // 3. Query items
    let items = db.get_recent_items(10).unwrap();
    assert_eq!(items.len(), 1);
    assert_eq!(items[0].title, Some("Integration Test".to_string()));
}
```

**Step 2: Run integration test**

Run: `cargo test -p cliphub integration_test`
Expected: PASS

**Step 3: Commit**

```bash
git add cliphub/
git commit -m "test(cliphub): add integration test"
```

---

### Task 12: README Documentation

**Files:**
- Create: `cliphub/README.md`

**Step 1: Write README**

```markdown
# ClipHub

Clipboard history manager with multi-platform sync.

## Features

- 📋 Automatic clipboard monitoring
- 💾 Local SQLite storage
- 🔍 Search and filter
- 🔄 Sync to Notion, self-hosted server, or Obsidian

## Building

```bash
cargo build --release
```

## Running

```bash
cargo run --bin cliphub
```

## Configuration

Create `cliphub.toml`:

```toml
[server]
url = "https://your-server.com"
api_key = "your-api-key"

[notion]
api_key = "your-notion-token"
database_id = "your-database-id"

[obsidian]
vault_path = "/path/to/vault"
```

## Development

```bash
# Run tests
cargo test

# Run with debug output
RUST_LOG=debug cargo run
```
```

**Step 2: Commit**

```bash
git add cliphub/
git commit -m "docs(cliphub): add README"
```

---

## Next Steps

After completing this plan:

1. **System Tray Integration** - Add background running capability
2. **Notion Sync** - Implement Notion API client
3. **Obsidian Sync** - Implement markdown file writing
4. **Image Support** - Handle clipboard images
5. **Advanced Search** - Add filters and sorting

---

**Plan Status:** Ready for execution
**Estimated Time:** 4-6 hours for MVP
**Dependencies:** Rust 1.88+, egui 0.33
